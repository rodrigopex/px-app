# QR-002: Review of OZ-002, OZ-004, OZ-006, OZ-008 fix (combined commit)

- **issue:** OZ-002, OZ-004, OZ-006, OZ-008
- **commit:** 59ba7e7
- **date:** 2026-03-15
- **verdict:** PASS

## Checklist

- [x] Generated C compiles cleanly — bare @synthesize and nil statics now produce valid C
- [x] Generated C is readable — struct fields match accessor references
- [x] Generated C is size-efficient — no dead code
- [x] Transpiler stdout shows clear progress/status — diagnostics emitted for bare @synthesize
- [x] Transpiler stderr gives actionable error — warning suggests explicit ivar form
- [x] LIMITATIONS.md updated — Properties section added, file-scope statics documented
- [x] Edge cases tested — GNUNullExpr, NullToPointer (implicit + cstyle), unsupported init
- [x] No regressions in existing golden tests — 299 transpiler tests passed
- [x] No regressions in existing behavior tests — 32 behavior tests passed
- [x] No regressions in adapted tests — 12 adapted tests passed
- [x] `--verbose` output includes diagnostics for both constructs

## Generated C Quality

### OZ-002 fix (resolve.py)

The fix correctly checks `existing_ivar_names` before defaulting to `_propName`.
When `@synthesize propName;` is used and Clang created an ivar named `propName`,
the accessor now references `self->propName` — matching the struct field. Clean,
minimal change (10 lines added to `_synthesize_properties`).

### OZ-004 fix (collect.py)

`_extract_init_value` now handles three nil representations from Clang AST:
- `GNUNullExpr` → `NULL`
- `ImplicitCastExpr` with `NullToPointer` → `NULL`
- `CStyleCastExpr` with `NullToPointer` → `NULL`

All three are common Clang AST representations of `nil`. The fix is thorough.
The ObjC type is already handled by existing `OZType` — transforms to
`struct ClassName *` automatically.

## Error Messaging (OZ-006)

Two diagnostic paths added:

1. **Bare @synthesize**: `"warning: '@synthesize sampleRate;' uses bare ivar
   name 'sampleRate' in class 'Config'; prefer '@synthesize sampleRate = _sampleRate;'"`
   — clear, actionable, names the class and property.

2. **Unsupported initializer**: `"warning: static variable '_shared' with ObjC type
   'PXAppConfig *' has unsupported initializer; declare without initializer and
   assign in +initialize"` — clear, actionable, suggests the workaround.

Both are warnings, not errors — the transpiler still processes what it can.
This matches the expected behavior from OZ-006.

## LIMITATIONS.md (OZ-008)

Two new entries added:
- **Properties section** (new): documents bare @synthesize behavior and the
  diagnostic warning
- **Types section**: documents file-scope statics with ObjC types, including
  the workaround pattern

Both entries are accurate and match the implemented behavior.

## Test Coverage

6 new tests across 2 files:

**test_collect.py** (4 tests):
- `test_static_with_gnu_null_init` — GNUNullExpr → NULL
- `test_static_with_null_to_pointer_cast` — ImplicitCastExpr NullToPointer
- `test_static_with_cstyle_null_cast` — CStyleCastExpr NullToPointer
- `test_static_objc_type_unsupported_init_warns` — diagnostic for CallExpr init

**test_resolve.py** (2 tests):
- `test_bare_synthesize_uses_existing_ivar` — ivar_name matches bare property
- `test_bare_synthesize_accessor_matches_struct` — synthesized getter references correct name

Coverage is solid. All nil AST variants tested, diagnostic paths tested.

## Regressions

Full suite: **343 passed, 0 failed**
- 299 transpiler tests (+4 new collect, +2 new resolve)
- 32 behavior tests
- 12 adapted tests

No regressions.

## Findings

None.

## Notes to MAINTAINER

Clean commit. All four QA-filed issues addressed correctly in a single commit.
The diagnostic messages are actionable and well-formatted. LIMITATIONS.md entries
are accurate. Test coverage is thorough for the nil variants.
