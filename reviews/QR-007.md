# QR-007: Review of OZ-007 + OZ-010 fix (enum collection + LIMITATIONS.md)

- **issue:** OZ-007, OZ-010
- **commit:** 8523e65
- **date:** 2026-03-15
- **verdict:** NEEDS_FOLLOWUP

## Checklist

- [x] Generated C compiles cleanly — enum declarations emitted in class header
- [x] Generated C is readable — enum definition placed before struct
- [x] Generated C is size-efficient — no impact
- [x] Transpiler stdout — N/A
- [x] Transpiler stderr — N/A
- [x] LIMITATIONS.md updated — switch/case entry corrected (OZ-010)
- [x] Edge cases tested — user enum from main file, enum ivar type
- [x] No regressions in transpiler tests — 305 passed
- [x] No regressions in behavior tests — 32 passed
- [x] No regressions in adapted tests — 12 passed

## Generated C Quality

The fix adds `_is_user_enum()` in `collect.py` — mirrors the existing
`_is_user_struct()` pattern. Filters out system enums, SDK enums, and
anonymous enums. User enums from the main file are collected into `type_defs`
and emitted in the class header.

Clean, consistent with the existing codebase pattern.

## LIMITATIONS.md (OZ-010)

Updated from:
> No `switch` / `case` statements. The transpiler does not handle
> `SwitchStmt` or `CaseStmt` AST nodes.

To: switch/case supported with C enum types. Accurate.

## Test Coverage

2 new tests: `test_user_enum_from_main_file`, `test_enum_ivar_type_emitted_in_header`.

## Regressions

Full suite: **349 passed, 0 failed**. No regressions.

## Findings

### Finding 1: [severity: high] OZ-009 — transpiler crash reported by DEV

DEV filed OZ-009 [URGENT] reporting `_is_user_enum` not defined at line 264.
However, QA review confirms `_is_user_enum` IS defined in commit `8523e65`
(line 155) and all 349 tests pass cleanly.

Possible causes:
1. DEV's `west update` pulled a partial/corrupt state
2. DEV tested before MAINTAINER finished committing
3. A file system caching issue

**Recommendation:** MAINTAINER should verify the commit is clean in the
canonical repo. DEV should `git pull origin manifest-rev` and retry.
If OZ-009 reproduces on a fresh checkout of `8523e65`, then there is a
real issue QA could not reproduce.

## Notes to MAINTAINER

- OZ-009 was a stale copy issue — resolved by DEV re-pulling.
- **OZ-007 is partial:** Enum collection works, but switch/case body emission
  does not — case labels and bodies are dropped, only condition expression
  remains. QA filed OZ-012 for the switch body gap and OZ-014 for the
  now-inaccurate LIMITATIONS.md entry.
- DEV cannot revert WA-004 fully — enum constants work in if/else, but
  switch/case still not usable.
