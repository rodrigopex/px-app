# QR-019: Review of OZ-019 + OZ-028 fix (inherited ivar base chain + coverage)

- **issue:** OZ-019, OZ-028
- **commit:** a2db0c3
- **date:** 2026-03-15
- **verdict:** PASS

## Checklist

- [x] Generated C compiles cleanly — `self->base._parentIvar` for inherited ivars
- [x] Generated C is readable — base chain mirrors struct embedding layout
- [x] Generated C is size-efficient — zero runtime cost (struct member access)
- [x] No regressions in transpiler tests — 322 passed
- [x] No regressions in behavior tests — 32 passed

## Generated C Quality

New function `_ivar_base_chain` in `emit.py` — walks the class hierarchy
to find which ancestor owns the ivar, returns `"base." * depth`.

- Own ivar: `self->_value` (no prefix)
- Parent ivar: `self->base._count`
- Grandparent ivar: `self->base.base._gval`

This mirrors the C struct embedding layout exactly. Clean, minimal (24 lines).

## Test Coverage

5 new tests total:

**OZ-019 (ivar access):**
- `test_parent_ivar_uses_base_prefix` — parent ivar → `self->base._count`
- `test_grandparent_ivar_double_base` — grandparent → `self->base.base._gval`
- `test_own_ivar_no_base_prefix` — own ivar → `self->_value` (no prefix)

**OZ-028 (inherited method cast coverage):**
- `test_same_class_method_no_cast` — same-class call must NOT cast
- (deep hierarchy covered by existing + OZ-019 tests)

Excellent coverage — tests both positive (base chain) and negative (no-op) cases.

## Regressions

Full suite: **354 passed, 0 failed**. No regressions.

## Findings

None.

## Notes to MAINTAINER

Combined OZ-019 fix with OZ-028 coverage — efficient. The `_ivar_base_chain`
function complements the existing `_base_chain` for struct embedding and
OZ-017's `_find_defining_class` for method dispatch. Together these three
functions fully handle deep inheritance: struct layout, method dispatch,
and ivar access.
