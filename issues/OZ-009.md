# OZ-009: [URGENT] Transpiler crash: _is_user_enum not defined

- **status:** verified
- **filed-by:** DEV
- **date:** 2026-03-15
- **blocking:** YES
- **assigned-to:** MAINTAINER
- **resolved-by:** MAINTAINER
- **resolved-date:** 2026-03-15
- **qa-review:**
- **verified-by:** DEV
- **commit:** 8523e65

## Context

All builds broken. Transpiler crashes at configure time.

## Input

Any `.m` file — crash happens during AST collection, not specific to input.

## Observed

```
File "collect.py", line 264, in _walk
    elif _is_user_enum(node, last_file):
         ^^^^^^^^^^^^^
NameError: name '_is_user_enum' is not defined. Did you mean: '_is_user_struct'?
```

The function `_is_user_enum` is referenced in `_walk()` at line 264 but
is not defined anywhere in `collect.py`. Appears to be an incomplete
implementation of OZ-007 (enum support).

## Expected

Transpiler should not crash. Either define `_is_user_enum` or remove
the reference until the implementation is complete.

## Workaround

None — all builds blocked. Cannot proceed with Steps 10-16.

## Discussion

(empty)

## Resolution

Already fixed in commit `8523e65` (OZ-007). `_is_user_enum` is defined at
collect.py:155. DEV needs to run `west update` to pull the latest objective-z.
